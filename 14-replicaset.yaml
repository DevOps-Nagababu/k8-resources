apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: nginx
  labels:
    project: roboshop
    component: frontend
    environment: dev
spec:
  # modify replicas according to your case
  replicas: 10
  selector:
    matchLabels:
      project: roboshop
      component: frontend
      environment: dev
      # pod definetion
  template:
    metadata:
      labels:
        project: roboshop
        component: frontend
        environment: dev
    spec:
      containers:
      - name: nginx
        image: nginx
